# RED instruction (derived-hypothesis depth)

You are a quantitative researcher operating under the `quant-research`
skill, which is the skill at:

`skills/quant-research/SKILL.md`

with references in `skills/quant-research/references/`. Read **at minimum**:

- `SKILL.md`
- `references/hypothesis_iteration.md`
- `references/hypothesis_generation.md`
- `references/purpose_design.md`
- `references/why_why_analysis.md`
- `references/cross_h_synthesis.md`

You inherit a notebook in progress. Its parent H1 has just been verdicted
**rejected**. The full experimental record (numbers, observations,
failure_mode label) is at:

`skills/quant-research/skill_tests/red_derived_hypothesis_depth/fixture_parent_H1.md`

Read that fixture in full.

## Your task

Generate up to **three** derived hypotheses (H2, H3, H4) to add to this
notebook (or the next notebook, if Purpose continuity routing demands it).

For each derived H, output:

1. **The H statement** in the form the protocol requires (falsifiable
   comparison with numeric thresholds).
2. **Pathway declaration** (1-6 or ad-hoc) per `hypothesis_generation.md`.
3. **`closes_conjuncts`** value per `hypothesis_iteration.md` sub-step 1.5.
4. **Routing decision** (same notebook vs. new notebook).
5. **One-paragraph justification** explaining *why this derived H is the
   right next experiment given what the parent experiment showed*.

Then output a **routing-decisions summary table** with the H2/H3/H4 rows.

## Constraints

- Follow the skill exactly. Do not skip pathway declaration.
- Do not propose a robustness sweep as an H — the protocol forbids that.
- Do **not** ask clarifying questions. Make reasonable assumptions where
  the fixture is silent.
- Output **only** the H statements and the routing summary table. Do not
  include meta-commentary about the skill, the fixture, or the process.

Begin.
